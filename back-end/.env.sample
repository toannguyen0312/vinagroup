# Environment
NODE_ENV=development

# Database URLs for different environments
DATABASE_URL=postgresql://username:password@localhost:5432/vinagroup_dev
DATABASE_URL_STAGING=postgresql://username:password@staging-host:5432/vinagroup_staging
DATABASE_URL_PRODUCTION=postgresql://username:password@production-host:5432/vinagroup_prod

# Server
PORT=5000

# For testing (optional - can use local test database)
# DATABASE_URL_TEST=postgresql://username:password@localhost:5432/vinagroup_test
